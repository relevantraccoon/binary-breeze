import { SxProps } from "@mui/system";

// Defines the styles for the button component
const buttonContentSx: SxProps = {
  padding: "1em",
  mb: "2em",
  mt: "1em",
};

export default buttonContentSx;
