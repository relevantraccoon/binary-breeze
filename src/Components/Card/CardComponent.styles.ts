import { SxProps } from "@mui/system";

// Defines the styles for the Card component
const cardSx: SxProps = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
  margin: "0 1em 1em 1em",
  padding: "0 1em 1em 1em",
};

export default cardSx;
