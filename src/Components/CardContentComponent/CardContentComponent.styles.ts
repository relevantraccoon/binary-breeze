import { SxProps } from "@mui/system";

// Defines the styles for the CardContent component
const cardContentSx: SxProps = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
  margin: "1em",
  padding: "1em",
};

export default cardContentSx;
